Example project that uses the homeremote-stream-player and homeremote-dockerlist React components and server plugins

After making changes, first build `yarn build` and then start the server `yarn start`.
